<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/follow_status"
        android:icon="@drawable/sync_now"
        android:title="Sync Now"
        android:onClick="scanDirectories"
        app:showAsAction="always">

    </item>

    <item android:id="@+id/follow_srefresh"
        android:icon="@drawable/refresh_table"
        android:title="Refresh"
        android:onClick="refreshCategory"
        app:showAsAction="always">

    </item>


    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:onClick="viewSettings"
        android:title="FTP Settings"
        app:showAsAction="never" />

</menu>
